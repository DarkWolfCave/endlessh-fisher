# Endlessh Fisher

Public release branch (Air-Gap pattern, see CM #145).
This branch holds squash-style release commits without internal history.

For active development see the `main` branch (private Gitea).
External users: this is the GitHub-mirrored branch.
