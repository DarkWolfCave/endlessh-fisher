# Endlessh Fisher - Production Docker Compose
#
# Simple production setup — exposes port 8000.
# Put your own reverse proxy (Nginx, Caddy, Traefik, etc.) in front.
#
# For a Traefik + Blue-Green setup, see docker-compose.prod.traefik.yml

x-backend-env: &backend-env
  - DJANGO_SETTINGS_MODULE=config.settings.production
  - SECRET_KEY=${SECRET_KEY}
  - ALLOWED_HOSTS=${ALLOWED_HOSTS}
  - ADMIN_URL=${ADMIN_URL}
  - POSTGRES_DB=${POSTGRES_DB:-endlessh_fisher}
  - POSTGRES_USER=${POSTGRES_USER:-endlessh_fisher}
  - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  - POSTGRES_HOST=postgres
  - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/1
  - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
  - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@redis:6379/0
  - INFLUXDB_URL=${INFLUXDB_URL}
  - INFLUXDB_TOKEN=${INFLUXDB_TOKEN}
  - INFLUXDB_ORG=${INFLUXDB_ORG}
  - INFLUXDB_BUCKET=${INFLUXDB_BUCKET}
  - SHOW_REAL_IP=${SHOW_REAL_IP:-false}
  - GAME_LANGUAGE=${GAME_LANGUAGE:-de}
  - ABUSEIPDB_API_KEY=${ABUSEIPDB_API_KEY:-}

x-backend-common: &backend-common
  build:
    context: .
    dockerfile: Dockerfile
    target: production
  restart: unless-stopped
  environment: *backend-env
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy

services:
  postgres:
    image: postgres:18-alpine
    container_name: endlessh_fisher_postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-endlessh_fisher}
      - POSTGRES_USER=${POSTGRES_USER:-endlessh_fisher}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-endlessh_fisher}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:8.6-alpine
    container_name: endlessh_fisher_redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    <<: *backend-common
    container_name: endlessh_fisher_backend
    ports:
      - "8100:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  celery_worker:
    <<: *backend-common
    container_name: endlessh_fisher_celery_worker
    command: celery -A config worker -l info --concurrency=2
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
    healthcheck:
      disable: true

  celery_beat:
    <<: *backend-common
    container_name: endlessh_fisher_celery_beat
    command: celery -A config beat -l info --schedule=/tmp/celerybeat-schedule
    healthcheck:
      disable: true

volumes:
  postgres_data:
  redis_data:
